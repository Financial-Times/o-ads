

require('isomorphic-fetch');

module.exports = {
	maxDuration: 1800,
	idleTimeout: 180,
	commandTimeout: 180,
	waitForConditionPollInterval: 300,
	waitForConditionTimeout : 60000,
	concurrency: 4
};
