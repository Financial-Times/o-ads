machine:
  node:
    version: 4.4.0
dependencies:
  override:
    - npm install
  cache_directories:
    - "node_modules"
test:
  override:
    - npm run ci
    - export CI=true ./node_modules/obt test --browserTest && unset CI
    - npm verify
