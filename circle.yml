machine:
  node:
    version: 4.4.0
dependencies:
  override:
    - npm install
  cache_directories:
    - "node_modules"
test:
  override:
    - ./node_modules/.bin/obt test --browserTest --browserTestFailBuild --environments=chrome_latest,chrome_latest-1,firefox_latest,firefox_latest-1
