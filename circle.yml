machine:
  node:
    version: 4.4.0
dependencies:
  override:
    - npm install
  cache_directories:
    - "node_modules"
test:
  override:
    - npm run ci
    - ./node_modules/.bin/obt test --browserTest
